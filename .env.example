# Phase 1 数据层环境变量示例
# 复制为 .env 并按需填写；禁止提交真实凭据。
# 本 Phase 1 数据层默认使用免费数据源（AKShare / BaoStock），不需要 API Key。
# 以下变量为预留，仅在后续阶段或自建数据源时使用。

# 主数据源 SDK（可选，仅在执行 fetch 时需要安装）
# pip install -e ".[sources]"

# 自建数据源 API Key 示例（预留，Phase 1 不使用）
# PRIVATE_DATA_API_KEY=

# BaoStock 无需凭据，但如需代理可设置
# HTTP_PROXY=
# HTTPS_PROXY=
